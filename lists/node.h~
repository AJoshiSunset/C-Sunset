#include <iostream>
#include <cstring>

using namespace std;


class node {
 public:
  node(student*);

  node* getNext();

  node* next;

  void setNext(node*);

  student* getStudent();

  student* stu;
};
