#include <iostream>
#include <cstring>

using namespace std;

#include "student.h"

Student::Student() {
  name = new char[50];
}

Student::Student(char* nameVal) {
  name = new char[50];
  strcpy(name, nameVal);
}

char* Student::getName() {
  return name;
}
